<% if @articles.count < 1 %>
    <p class="no_blog">No blogs created yet</p>

    <% else %>

        <%= render 'articles' %>
            <div class="apple_pagination mt-4">
                <%= will_paginate @articles, :container=> false %>
            </div>

            <% end %>